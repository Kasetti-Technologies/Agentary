# -----------------------------
# Agentary Test Environment
# -----------------------------

# PostgreSQL Database Connection
# This points to the Docker container we started.
DATABASE_URL=postgresql://postgres:mysecretpassword@localhost:5432/agentary_test

# Staging API URL for E2E tests
# A placeholder for tests that make network requests.
STAGING_API_URL=http://localhost:3000/api/v1/usage-events

# Kafka Broker Connection
# Tells the application where to find the Kafka service.
KAFKA_BROKER=localhost:9092

# JWKS URI for Token Validation
# Used by the tenantContext middleware to validate JWTs.
JWKS_URI=https://your-auth-provider.com/.well-known/jwks.json

# KMS Signing Key ID for Artifact Signer
# A placeholder key ID for the artifact signing service.
SIGNING_KEY_ID=abc-123-local-key
